import { SEARCH_FETCH_RESULTS, SEARCH_RESULTS_UPDATED } from '../constants';

const results = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default results;